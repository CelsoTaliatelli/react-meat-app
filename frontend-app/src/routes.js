import {
    BrowserRouter as Router,
    Switch,
    Route,
    Link
} from "react-router-dom";

const routes = [
    {
        path: "/components",
        component:RestaurantMenu
    }
]

export default routes;
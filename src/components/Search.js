import { TextField } from "@material-ui/core";

export default function Search() {
    return (
        <TextField
            id="outlined-full-width"
            placeholder="Buscar..."
            fullWidth
            variant="outlined">
         </TextField>
    );

}